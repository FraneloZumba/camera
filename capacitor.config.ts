import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.camerafranxd.app',
  appName: 'camerafranxd',
  webDir: 'dist/myapp/browser', 
};

export default config;
